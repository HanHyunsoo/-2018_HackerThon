<%= form_for([@board, @article], :multipart => true) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, '제목' %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content, '내용' %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :img, '이미지' %>
    <%= f.file_field :img %>
  </div>
  
  <div class="actions">
    <%= f.submit '글쓰기', :class => 'button' %>
  </div>
<% end %>
