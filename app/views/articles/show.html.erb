<div class="container">
  <p id="notice"><%= notice %></p>
  
  <p>
    <strong>Title:</strong>
    <%= @article.title %>
  </p>
  
  <p>
    <strong>Content:</strong>
    <%= @article.content %>
  </p>
  <p>
    <strong>사진:</strong>
    <%= image_tag("#{@article.img_url}")%>
  </p>


  <%= link_to 'Edit', edit_article_path(@board, @article) %> |
  <%= link_to 'Back', articles_path %> |
  <%= link_to 'Destroy', [@board, @article] , method: :delete ,data: { confirm: 'Are you sure?' } %></td>
</div>